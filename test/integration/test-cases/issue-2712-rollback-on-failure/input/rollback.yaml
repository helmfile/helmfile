releases:
- name: issue-2712-rollback
  chart: ../../../charts/raw
  rollbackOnFailure: true
  values:
  - templates:
    - |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: {{ .Release.Name }}-cm
        namespace: {{ .Release.Namespace }}
      data:
        version: "1"
