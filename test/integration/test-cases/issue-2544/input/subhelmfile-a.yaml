releases:
- name: release-a
  chart: ../../../charts/raw
  labels:
    app: a
  values:
  - templates:
    - |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: release-a
        namespace: {{ .Release.Namespace }}
      data:
        group: a
